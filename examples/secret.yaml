apiVersion: v1
kind: Secret
metadata:
  name: hcloud-token
  namespace: karpenter
type: Opaque
stringData:
  # Replace with your Hetzner Cloud API token. Generate at
  # https://console.hetzner.cloud/<project>/security/tokens
  # Required scopes: Read & Write on the project.
  token: REPLACE_ME
